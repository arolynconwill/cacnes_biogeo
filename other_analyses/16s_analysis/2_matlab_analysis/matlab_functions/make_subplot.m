function make_subplot( subplot_size_x, subplot_size_y, subplot_num, these_samples_name, bar_temp, x_labels, graph_labels, legend_bool )

subplot( subplot_size_x, subplot_size_y, subplot_num )
make_miniplot( these_samples_name, bar_temp, x_labels, graph_labels, legend_bool )

end